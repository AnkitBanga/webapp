apiVersion: v1
kind: Service
metadata:
  name: testmywebapp
  namespace: default
  labels:
    app: mywebapp
spec:
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30080
  selector:
    app: mywebapp
  type: NodePort

